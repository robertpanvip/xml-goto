<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <!-- Unique identifier of the plugin. It should be FQN. It cannot be changed between the plugin versions. -->
    <id>com.pan.xml</id>

    <!-- Public plugin name should be written in Title Case.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/plugin-overview-page.html#plugin-name -->
    <name>DOM attribute/tag navigation</name>

    <!-- A displayed Vendor name or Organization ID displayed on the Plugins Page. -->
    <vendor url="https://github.com/robertpanvip/xml-goto">pan</vendor>
    <!-- Description of the plugin displayed on the Plugin Page and IDE Plugin Manager.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/plugin-overview-page.html#plugin-description -->
    <description><![CDATA[
<h3>🎯 Core Functionality</h3>
DOM Attribute Navigation enables precise jump-to-definition for HTML attributes and tag names in XML/Vue template files (including .vue Single-File Components). Supports Ctrl+Click to navigate directly to the corresponding DOM type declarations in lib.dom.d.ts.

<h3>✨ Supported Scenarios</h3>
<ul>
  <li><strong>Plain HTML Attributes</strong>: e.g., <code>&lt;div title="xxx"&gt;</code> → Jumps to <code>HTMLElement.title: string</code></li>
  <li><strong>Bound Attributes (Vue)</strong>: e.g., <code>&lt;div :title="xxx"&gt;</code> → Jumps to <code>HTMLElement.title: string</code></li>
  <li><strong>Special Attribute Mapping</strong>: e.g., <code>class/:class</code> → Automatically maps to <code>Element.className: string</code></li>
  <li><strong>Event Bindings</strong>: e.g., <code>&lt;div onclick=""&gt;</code> (XML) / <code>&lt;div @click=""&gt;</code> (Vue) → Jumps to <code>GlobalEventHandlers.onclick</code></li>
  <li><strong>Tag Name Navigation</strong>: Ctrl+Click on <code>&lt;div&gt;</code> (XML/Vue) → Jumps to <code>HTMLDivElement</code> interface</li>
  <li><strong>Deep Inheritance Chain</strong>: Resolves full DOM inheritance (HTMLDivElement → HTMLElement → Element → GlobalEventHandlers, etc.)</li>
</ul>

<h3>🚀 Key Features</h3>
<ul>
  <li>✅ Universal Support: Works for both standard XML files and Vue .vue template sections</li>
  <li>✅ Precise Positioning: Directly navigates to the exact line of attribute definition with full TypeScript type signature</li>
  <li>✅ Syntax Compatibility: Auto-normalizes Vue-specific syntax (:/v-bind, @/v-on, modifiers like .stop/.once) while maintaining XML native support</li>
  <li>✅ Zero Configuration: Ready to use immediately after installation, no extra setup required</li>
</ul>
]]></description>

    <!-- Product and plugin compatibility requirements.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html -->
    <depends>JavaScript</depends>
    <depends>com.intellij.modules.platform</depends>
    <!-- 关键：声明你的 ReferenceContributor -->
    <extensions defaultExtensionNs="com.intellij">
       <!-- 如果你还支持 TypeScript，可以再加一行 language="TypeScript" -->
        <gotoDeclarationHandler implementation="com.pan.xml.VueAttrGotoDeclarationHandler" />
    </extensions>
</idea-plugin>